[package]
name = "snow-native"
version = "0.1.1"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3", features = ["napi9"] }
napi-derive = "3"
encoding_rs = "0.8"
chardetng = "0.1"
