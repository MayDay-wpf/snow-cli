name: Publish to NPM

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 1.0.0)'
        required: true
        type: string

permissions:
  contents: write
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org/'
          
      - name: Install dependencies
        run: npm install --legacy-peer-deps
        
      - name: Build project
        run: npm run build
        
      - name: Update version (manual trigger)
        if: github.event_name == 'workflow_dispatch'
        run: npm version ${{ github.event.inputs.version }} --no-git-tag-version
        
      - name: Publish to NPM
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          
      - name: Create GitHub Release
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## 🚀 Snow CLI ${{ github.ref_name }}
            
            ### What's New
            -- Added ConfigProfileScreen for managing user profiles, including creating, renaming, deleting, and switching profiles.
            -- Enhanced ConfigScreen to support profile selection and management, allowing users to create and delete profiles directly from the configuration settings.
            -- Introduced configManager utility for handling profile operations such as loading, saving, and switching profiles.
            -- Updated apiConfig to synchronize active profile settings with the main configuration.
            -- Improved user experience by providing clear prompts and error messages during profile management actions.
            -- Refactored existing code to accommodate the new profiles feature, ensuring backward compatibility with legacy configurations.
            ### Installation
            ```bash
            npm install -g snow-ai
            ```
            
            ### Usage
            ```bash
            snow
            ```
            
            See [CHANGELOG.md](./CHANGELOG.md) for detailed changes.
          draft: false
          prerelease: false