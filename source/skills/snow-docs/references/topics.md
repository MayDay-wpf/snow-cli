# Topic index (progressive disclosure)

Do not paste this whole file into replies. Use tools:

1. `snow-docs-search` with a topic keyword
2. `snow-docs-get` with a single document id

## High-traffic topics

- Installation → search `install` / `安装`
- First configuration / Profile → `profile` / `首次配置`
- MCP → `mcp`
- Skills → `skills`
- Hooks → `hooks`
- Sub-agents → `subagent` / `子代理`
- Sensitive commands → `sensitive` / `敏感`
- Proxy & browser → `proxy` / `browser`
- Third-party relay → `relay` / `中转`
- LSP / ACE → `lsp` / `ace`
- Team mode → `team`
- SSE mode → `sse`
- Privacy → `privacy` / `隐私`

Full catalogue: `snow-docs-list`.

User-facing docs pages (also available via tools):

- zh: `28.官方文档工具snow-docs.md`
- en: `28.Official Docs Tools snow-docs.md`
